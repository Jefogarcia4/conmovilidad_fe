# URL base de la API. Si se omite, el cliente usa la ruta relativa /api, que en desarrollo
# resuelve el proxy de Vite hacia http://localhost:5153 (ver vite.config.ts).
# En despliegue, apúntala a la API publicada, por ejemplo:
# VITE_API_URL=https://api.conmovilidad.com/api
VITE_API_URL=
